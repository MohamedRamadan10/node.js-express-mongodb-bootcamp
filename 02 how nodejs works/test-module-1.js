"use strict";

// class Calculator {
// 	add(x, y) {
// 		return x + y;
// 	}

// 	minus(x, y) {
// 		return x - y;
// 	}

// 	divide(x, y) {
// 		return x / y;
// 	}
// }

// module.exports = Calculator;

module.exports = class {
	add(x, y) {
		return x + y;
	}

	minus(x, y) {
		return x - y;
	}

	divide(x, y) {
		return x / y;
	}
};
